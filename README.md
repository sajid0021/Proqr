ProQR Studio

Repository reorganized into `client/` (React + Vite) and `server/` (Express) folders.

Quick start:

- Install server deps: `cd server && npm install`
- Run server: `cd server && npm start`
- Install client deps: `cd client && npm install`
- Run client: `cd client && npm run dev`

Build client: `cd client && npm run build`
